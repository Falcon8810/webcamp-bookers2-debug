<% if book.favorited_by?(current_user) %>
  <%= link_to book.favorites.count, book_favorites_path(book), method: :delte, class: "fa fa-heart heart-red" %>
<% else %>
  <%= link_to book.favorites.count, book_favorites_path(book), method: :post, class: "fa fa-heart heart-blue" %>
<% end %>